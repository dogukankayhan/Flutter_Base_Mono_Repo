name: flutter_kit_network
description: Networking layer — Dio client, interceptors, caching, offline queue.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.9.0
  connectivity_plus: ^7.0.0
  shared_preferences: ^2.5.4
  sqflite: ^2.3.0
  path: ^1.8.3
  get_it: ^9.2.0
  mockito: ^5.6.4
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
