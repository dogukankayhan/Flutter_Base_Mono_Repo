name: flutter_kit_core
description: Core abstractions — BaseBloc, BaseState, BaseUseCase, BaseRepository, validators.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_kit_network:
    path: ../flutter_kit_network

  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  url_launcher_platform_interface: ^2.3.2
